# 작업 보고서 - 2026-03-27 (Work14)

## 작업 내용: 질문 데이터 서버 이전 및 추가 100개 작성

**Base URL**: `https://1cq1kfgvf1.execute-api.ap-northeast-2.amazonaws.com`

---

## 수행 내용

### 1. 기존 질문 100개 서버 DB에 이전
`npm run db:seed` 실행으로 기존 `prisma/seed.ts`의 질문 100개를 RDS에 삽입

### 2. 세대 간 소통 질문 100개 추가 작성
20대 자녀 ↔ 50~60대 부모가 서로 궁금한 것을 주제로 5개 카테고리에 걸쳐 100개 작성

| 카테고리 | 추가 수 | 주요 주제 |
|---------|--------|----------|
| DAILY (일상) | 25개 | 일상 공유, 취미, 건강, 소통 |
| MEMORY (추억) | 20개 | 젊은 시절, 첫 직장, 자녀 성장, 부모님 기억 |
| VALUE (가치관) | 20개 | 세대 차이, 인생 교훈, 후회, 전달하고 싶은 말 |
| DREAM (꿈/목표) | 20개 | 은퇴 후 삶, 가족과 함께 이루고 싶은 것 |
| GRATITUDE (감사) | 15개 | 부모·자녀 간 감사, 가족의 의미 |

### 3. seed.ts 로직 개선
- 기존: 100개 이상이면 무조건 스킵
- 변경: 기존 질문 수와 전체 questions 배열 크기를 비교하여 새 질문만 추가 삽입

```ts
// 추가 실행: 기존에 없는 질문만 삽입 (content 기준 중복 체크)
const existingContents = new Set(...);
const newQuestions = questions.filter(q => !existingContents.has(q.content));
```

---

## 최종 DB 상태

| 항목 | 수량 |
|------|------|
| 기존 질문 (seed v1) | 100개 |
| 추가 질문 (seed v2) | 100개 |
| **총 질문 수** | **200개** |

---

## 테스트 결과

- `GET /questions/today`: 새로 추가된 질문 중 하나 정상 반환 ✅
- 전체 질문이 풀(pool)로 사용되어 랜덤 배정 정상 동작 확인 ✅

---

## 수정 파일

- `prisma/seed.ts` — 질문 100개 추가 및 시드 로직 개선
